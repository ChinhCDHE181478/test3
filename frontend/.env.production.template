# ================================================================
# FRONTEND - PRODUCTION ENVIRONMENT TEMPLATE
# ================================================================
# Copy file này thành .env.production và điền giá trị thật
# 
# LƯU Ý QUAN TRỌNG:
# - Next.js chỉ đọc biến có prefix NEXT_PUBLIC_ ở client-side
# - Thay tất cả YOUR_DOMAIN_HERE bằng domain thật
# ================================================================

# ----------------
# API URLs - Internal (Docker network)
# ----------------
# Dùng khi các service giao tiếp nội bộ qua Docker network
# NEXT_PUBLIC_API_URL=http://backend:8080/api/v1
# NEXT_PUBLIC_AGENT_API=http://agents:4000

# ----------------
# API URLs - External (qua Nginx reverse proxy)
# ----------------
# Dùng khi frontend cần gọi API từ browser (client-side)
# THAY YOUR_DOMAIN_HERE bằng domain thật của bạn
NEXT_PUBLIC_API_URL=https://vivuplan.io.vn/api/v1
NEXT_PUBLIC_AGENT_API=https://vivuplan.io.vn/agents

# Secret cho cron prewarm (api/hotels/domestic/prewarm)
# Ví dụ: openssl rand -hex 32
CRON_SECRET=

# ================================================================
# KHI NÀO DÙNG INTERNAL VS EXTERNAL?
# ================================================================
# - INTERNAL (http://backend:8080): Khi Next.js server-side gọi API
# - EXTERNAL (https://vivuplan.io.vn): Khi browser (client) gọi API
# 
# Với Next.js 15, hầu hết requests từ browser nên dùng EXTERNAL URLs
# ================================================================
